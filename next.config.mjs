/** @type {import('next').NextConfig} */
const nextConfig = {};

export default {
  images: {
    domains: [
      'images.unsplash.com',
      'i.pinimg.com',
      // add other domains as needed
    ],
  },
};
